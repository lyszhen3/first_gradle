#server:
#  ssl:
#    key-store: classpath:sample.jks
#    key-store-password: secret
#    key-password: password
logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO

#spring:
#  security:
#    user:
#      name: user
#      password: password
spring:
  profiles:
    active: pro
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/demo?useUnicode=true&characterEncoding=utf8&autoReconnect=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
